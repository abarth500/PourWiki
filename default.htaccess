
<Location /pourwiki>
# Overwrite the path to your AuthUserFile
	AuthUserFile /var/www/html/.htpasswd
	AuthGroupFile /dev/null
	AuthName "Input ID and Password."
	AuthType Basic
	require valid-user
	<Files ~ "^.(htpasswd|htaccess)$">
		deny from all
	</Files>
</Location>